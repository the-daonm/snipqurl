apiVersion: apps/v1
kind: Deployment
metadata:
  name: snipqurl
  namespace: snipqurl
  labels:
    app: snipqurl
spec:
  replicas: 1
  selector:
    matchLabels:
      app: snipqurl
  template:
    metadata:
      annotations:
        prometheus.io/scrape: "true"
        prometheus.io/port: "8080"
        prometheus.io/path: "/metrics"
      labels:
        app: snipqurl
    spec:
      containers:
        - name: snipqurl
          image: dos382/snipqurl:b506ceea0c591ab3a0925d9b777c603467287915
          ports:
            - containerPort: 8080
          envFrom:
            - configMapRef:
                name: snipqurl-configmap
            - secretRef:
                name: snipqurl-secret
          readinessProbe:
            httpGet:
              path: /
              port: 8080
            initialDelaySeconds: 5
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /
              port: 8080
            initialDelaySeconds: 15
            periodSeconds: 20
          resources:
            requests:
              cpu: 100m
              memory: 128Mi
            limits:
              cpu: 500m
              memory: 512Mi
